package base;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public abstract class BaseFunc {
    public static Logger LOGGER = LogManager.getLogger(BaseFunc.class);
}
